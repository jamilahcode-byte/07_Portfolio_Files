# Store a sentence in a variable
text = "Python makes data clean and simple"

# Split the sentence into a list of words using spaces
words = text.split()

# Count how many words are in the list and print the result
print(len(words))